{
  "name": "@formalcore/langgraph-interceptors",
  "version": "0.1.0",
  "description": "Drop-in for LangChain / LangGraph agents: run every tool call through the reference MCP interceptors (SEP-2624), emit provenance labels, and produce an offline-verifiable attested denial when a read-then-send exfiltration is attempted.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "demo": "vite-node examples/relaybleed-langgraph.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "interceptors",
    "sep-2624",
    "langchain",
    "langgraph",
    "provenance",
    "information-flow-control",
    "attestation"
  ],
  "engines": {
    "node": ">=20"
  },
  "peerDependencies": {
    "@langchain/core": ">=0.3.0 <2"
  },
  "devDependencies": {
    "@langchain/core": "^1.1.0",
    "@langchain/langgraph": "^1.0.2",
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0",
    "vite-node": "^2.0.0",
    "vitest": "^2.0.0",
    "zod": "^3.23.8"
  }
}
